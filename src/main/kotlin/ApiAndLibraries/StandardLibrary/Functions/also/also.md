# also()
## What is also()?
`also()` is a function that lets you perform additional actions on an object and then return the same object
## Anatomy
- "here's an object, do something with it, and then give it back to me"
## The Syntax
```
object.also{
    // do something with the object
}
// returns the original object
```