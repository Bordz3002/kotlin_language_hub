# And
## What is and()?
`and` is a function that performs a logical AND operation on two Boolean values. It returns `true` only if both values are `true`
## Analogy:
- "Are both of these things true?. if yes, return true. Otherwise return false"