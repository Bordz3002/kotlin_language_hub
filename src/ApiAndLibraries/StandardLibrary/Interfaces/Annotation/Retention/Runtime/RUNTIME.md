# What is `RUNTIME` Retention?
An Annotation with `RUNTIME` is available at runtime and be accessed via reflection.  
This is the most flexible retention policy because your running program can read and use the annotation
## Analogy
"This annotation stays with the program and can be read while its running"  
This annotation stays with the program throughout its lifecycle