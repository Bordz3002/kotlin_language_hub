# What is `SOURCE` Retention?
An annotation with `SOURCE` retention is only available in the source code  
The compiler removes it completely during compilation, so it doesnt exist in the compiled class files or at runtime
## Analogy
"This annotation is just a note for the developer and the compiler - it disappears after compilation"